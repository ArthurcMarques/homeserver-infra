services:
  demo-climagyn:
    image: nginx:stable
    container_name: demo-climagyn
    restart: unless-stopped

    volumes:
      - ./html:/usr/share/nginx/html:ro

    networks:
      - proxy

networks:
  proxy:
    external: true